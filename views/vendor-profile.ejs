<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vendor Profile - AgriMarket</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background: #f8faf7; }
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .navbar-nav .nav-link { color: #2d5a27 !important; font-weight: 500; }
        .navbar-nav .nav-link:hover { color: #1a3d1a !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1a3d1a; border-color: #1a3d1a; }
        .footer { background-color: #2d5a27; color: white; padding: 20px 0; margin-top: 50px; }
        .profile-card, .edit-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44, 90, 39, 0.10);
            padding: 32px 28px 28px 28px;
            margin-bottom: 32px;
            border: none;
            transition: box-shadow 0.2s;
        }
        .profile-card:hover, .edit-card:hover {
            box-shadow: 0 8px 32px rgba(44, 90, 39, 0.16);
        }
        .profile-avatar {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background: linear-gradient(135deg, #e8f5e9 0%, #b7e4c7 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 18px auto;
            box-shadow: 0 2px 8px rgba(44, 90, 39, 0.07);
            animation: popIn 0.7s cubic-bezier(.68,-0.55,.27,1.55);
        }
        @keyframes popIn {
            0% { transform: scale(0.7); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }
        .profile-avatar i {
            font-size: 2.7rem;
            color: #2d5a27;
        }
        .profile-username {
            font-size: 1.4rem;
            font-weight: 700;
            color: #2d5a27;
            text-align: center;
        }
        .profile-email {
            color: #388e3c;
            font-size: 1rem;
            margin-bottom: 18px;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        .profile-email-badge {
            background: #e8f5e9;
            color: #2d5a27;
            border-radius: 8px;
            padding: 2px 10px;
            font-size: 0.95rem;
            font-weight: 500;
        }
        .profile-divider {
            border-top: 2px dashed #b7e4c7;
            margin: 24px 0 18px 0;
        }
        .profile-stats {
            text-align: center;
            margin-bottom: 0;
        }
        .profile-section-title {
            color: #2d5a27;
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 12px;
            letter-spacing: 0.5px;
        }
        .form-label {
            font-weight: 500;
            color: #2d5a27;
        }
        .form-control {
            border-radius: 8px;
            border: 1px solid #b5c7b3;
            transition: box-shadow 0.2s, border-color 0.2s;
        }
        .form-control:focus {
            border-color: #2d5a27;
            box-shadow: 0 0 0 2px #b7e4c7;
        }
        .btn-success {
            background-color: #2d5a27;
            border-color: #2d5a27;
        }
        .btn-success:hover {
            background-color: #1a3d1a;
            border-color: #1a3d1a;
        }
        @media (max-width: 600px) {
            .profile-card, .edit-card {
                padding: 18px 6px 12px 6px;
            }
        }
    </style>
</head>
<body>
    <!-- Vendor Navbar -->
    <%- include('vendor-navbar') %>
    <main class="container-fluid my-5 px-2" style="padding-top: 70px; max-width: 600px;">
        <h1 class="mb-4"><i class="fas fa-user me-2"></i>Vendor Profile</h1>
        <div class="profile-card">
            <div class="profile-avatar mb-2">
                <i class="fas fa-user"></i>
            </div>
            <div class="profile-username"><%= user.username %></div>
            <div class="profile-email"><i class="fas fa-envelope me-1"></i> <span class="profile-email-badge"><%= user.email %></span></div>
            <div class="profile-divider"></div>
            <div class="profile-stats">
                <div class="profile-section-title">Account Stats</div>
                <p><strong>Products Listed:</strong> <%= user.products ? user.products.length : 0 %></p>
                <p><strong>Customers:</strong> <%= user.customers ? user.customers.length : 0 %></p>
            </div>
        </div>
        <div class="edit-card">
            <div class="profile-section-title mb-3"><i class="fas fa-pen me-2"></i>Edit Profile</div>
            <form action="/vendor/profile/edit" method="POST">
                <div class="mb-3">
                    <label for="username" class="form-label">Username</label>
                    <input type="text" class="form-control" id="username" name="username" value="<%= user.username %>" required>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" required>
                </div>
                <button type="submit" class="btn btn-success w-100"><i class="fas fa-save me-1"></i>Save Changes</button>
            </form>
        </div>
    </main>
    <footer class="footer">
        <div class="container text-center">
            <p>&copy; 2024 AgriMarket. All rights reserved.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 