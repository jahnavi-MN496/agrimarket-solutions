<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product - AgriMarket Vendor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: #f8faf7;
        }
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .navbar-nav .nav-link { color: #2d5a27 !important; font-weight: 500; }
        .navbar-nav .nav-link:hover { color: #1a3d1a !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1a3d1a; border-color: #1a3d1a; }
        .footer { background-color: #2d5a27; color: white; padding: 20px 0; margin-top: 50px; }
        .add-product-card {
            background: #fff;
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(44, 90, 39, 0.10);
            padding: 40px 32px 32px 32px;
            margin: 0 auto;
            max-width: 500px;
        }
        .add-product-card h1 {
            font-size: 1.6rem;
            font-weight: 700;
            color: #2d5a27;
            margin-bottom: 28px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .add-product-card label {
            font-weight: 600;
            color: #2d5a27;
        }
        .add-product-card input,
        .add-product-card select,
        .add-product-card textarea {
            border-radius: 10px;
            border: 1px solid #b5c7b3;
            margin-bottom: 18px;
            background: #f8faf7;
        }
        .add-product-card input:focus,
        .add-product-card select:focus,
        .add-product-card textarea:focus {
            border-color: #2d5a27;
            box-shadow: 0 0 0 2px #b7e4c7;
        }
        .add-product-card .btn-primary {
            font-size: 1.1rem;
            font-weight: 600;
            padding: 10px 0;
            border-radius: 10px;
        }
        .form-label {
            font-weight: 500;
            color: #2d5a27;
        }
        .form-control, .form-select, textarea {
            border-radius: 8px;
            border: 1px solid #b5c7b3;
            transition: box-shadow 0.2s, border-color 0.2s;
        }
        .form-control:focus, .form-select:focus, textarea:focus {
            border-color: #2d5a27;
            box-shadow: 0 0 0 2px #b7e4c7;
        }
        .form-check-label {
            color: #2d5a27;
        }
        .add-product-icon {
            color: #2d5a27;
            font-size: 2rem;
        }
        @media (max-width: 600px) {
            .add-product-card {
                padding: 24px 8px 16px 8px;
            }
        }
    </style>
</head>
<body>
    <!-- Vendor Navbar -->
    <%- include('vendor-navbar') %>
    <main class="container-fluid my-5 px-2" style="padding-top: 70px; max-width: 600px;">
        <div class="add-product-card">
            <h1><i class="fas fa-plus me-2"></i>Add New Product</h1>
            <form action="/vendor/products/add" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="name" class="form-label">Product Name</label>
                    <input type="text" class="form-control" id="name" name="name" required placeholder="e.g. Fresh Apples">
                </div>
                <div class="mb-3">
                    <label for="image" class="form-label">Product Image</label>
                    <input type="file" class="form-control" id="image" name="image" accept="image/*" required>
                </div>
                <div class="mb-3">
                    <label for="category" class="form-label">Category</label>
                    <select class="form-select" id="category" name="category" required>
                        <option value="">Select Category</option>
                        <option value="Vegetables">Vegetables</option>
                        <option value="Fruits">Fruits</option>
                        <option value="Dairy">Dairy</option>
                        <option value="Bakery">Bakery</option>
                        <option value="Grains">Grains</option>
                        <option value="Spices">Spices</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Price ($)</label>
                    <input type="number" class="form-control" id="price" name="price" step="0.01" min="0" required placeholder="e.g. 2.99">
                </div>
                <div class="mb-3">
                    <label for="stock" class="form-label">Stock</label>
                    <input type="number" class="form-control" id="stock" name="stock" min="0" required placeholder="e.g. 50">
                </div>
                <div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" id="available" name="available" checked>
                    <label class="form-check-label" for="available">Available</label>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea class="form-control" id="description" name="description" rows="3" placeholder="e.g. Fresh, organic apples from local farms"></textarea>
                </div>
                <button type="submit" class="btn btn-primary w-100">Add Product</button>
            </form>
        </div>
    </main>
    <footer class="footer">
        <div class="container text-center">
            <p>&copy; 2024 AgriMarket. All rights reserved.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 