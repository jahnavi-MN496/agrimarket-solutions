<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AgriMarket - Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .navbar-brand {
            font-weight: bold;
            color: #2d5a27 !important;
        }
        .navbar-nav .nav-link {
            color: #2d5a27 !important;
            font-weight: 500;
        }
        .navbar-nav .nav-link:hover {
            color: #1a3d1a !important;
        }
        .search-form {
            max-width: 400px;
        }
        .btn-primary {
            background-color: #2d5a27;
            border-color: #2d5a27;
        }
        .btn-primary:hover {
            background-color: #1a3d1a;
            border-color: #1a3d1a;
        }
        .footer {
            background-color: #2d5a27;
            color: white;
            padding: 20px 0;
            margin-top: 50px;
        }
        .profile-card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 6px 32px rgba(44, 90, 39, 0.13);
            background: #fff;
            overflow: hidden;
            margin-top: 40px;
        }
        .profile-header {
            background: linear-gradient(120deg, #e8f5e9 0%, #a5d6a7 100%);
            color: #2d5a27;
            padding: 2.5rem 1rem 2.5rem 1rem;
            text-align: center;
        }
        .profile-avatar {
            width: 110px;
            height: 110px;
            border-radius: 50%;
            border: 4px solid #43a047;
            box-shadow: 0 4px 18px rgba(44,90,39,0.13);
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.2rem auto;
            position: static;
        }
        .profile-avatar svg {
            width: 70px;
            height: 70px;
            color: #43a047;
        }
        .profile-header h4 {
            margin-top: 0.5rem;
            font-weight: 700;
            letter-spacing: 0.5px;
            color: #2d5a27;
        }
        .profile-header p {
            color: #388e3c;
            font-size: 1.1rem;
        }
        .profile-form label {
            font-weight: 500;
            color: #2d5a27;
        }
        .profile-form .input-group {
            margin-bottom: 1.2rem;
        }
        .profile-form input,
        .profile-form textarea {
            border-radius: 10px;
            border: 1.5px solid #c8e6c9;
            background: #f8faf7;
            box-shadow: 0 1px 6px rgba(44,90,39,0.07);
            transition: border-color 0.2s, box-shadow 0.2s;
            font-size: 1.05rem;
            color: #2d5a27;
        }
        .profile-form input:focus,
        .profile-form textarea:focus {
            border-color: #43a047;
            background: #e8f5e9;
            box-shadow: 0 2px 12px rgba(44,90,39,0.13);
        }
        .profile-form input[readonly],
        .profile-form textarea[readonly] {
            background: #f8faf7;
            color: #789262;
            border-style: solid;
        }
        .input-group-text {
            background: #e8f5e9;
            border: none;
            color: #43a047;
            font-size: 1.1rem;
            border-radius: 10px 0 0 10px;
        }
        .profile-actions {
            margin-top: 1.5rem;
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
        }
        .alert-success {
            border-radius: 8px;
            font-size: 1rem;
        }
        @media (max-width: 600px) {
            .profile-card { padding: 0.5rem; }
            .profile-header { padding: 2.5rem 0.5rem 1.5rem 0.5rem; }
            .profile-avatar { width: 80px; height: 80px; }
            .profile-header h4 { margin-top: 1.5rem; }
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/customer/products">
                <i class="fas fa-leaf me-2"></i>AgriMarket
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/customer/products">
                            <i class="fas fa-store me-1"></i>Products
                        </a>
                    </li>
                </ul>
                
                <ul class="navbar-nav d-flex align-items-center gap-3">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li class="nav-item">
                            <a class="nav-link active" href="/customer/profile">
                                <i class="fas fa-user me-1"></i>Profile
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">
                                <i class="fas fa-sign-out-alt me-1"></i>Logout
                            </a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">
                                <i class="fas fa-sign-in-alt me-1"></i>Login
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/signup">
                                <i class="fas fa-user-plus me-1"></i>Sign Up
                            </a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/customer/cart">
                            <i class="fas fa-shopping-cart me-1"></i>Cart
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/customer/orders">
                            <i class="fas fa-list me-1"></i>Orders
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container-fluid my-2 px-2" style="padding-top: 70px;">
        <div class="row justify-content-center">
            <div class="col-12 col-md-8">
                <div class="card profile-card">
                    <div class="profile-header">
                        <div class="profile-avatar" style="background: #e8f5e9;">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <circle cx="12" cy="8" r="4" stroke-width="1.5" fill="#43a047" />
                                <path stroke-width="1.5" d="M4 20c0-3.313 3.134-6 7-6s7 2.687 7 6" stroke="#43a047" />
                            </svg>
                        </div>
                        <h4><%= user ? user.username : 'John Doe' %></h4>
                        <p><%= user ? user.email : 'Customer' %></p>
                    </div>
                    <div class="card-body p-4">
                        <% if (typeof profileUpdated !== 'undefined' && profileUpdated) { %>
                            <div class="alert alert-success">Profile updated successfully!</div>
                        <% } %>
                        <form action="/customer/profile" method="POST" class="profile-form">
                            <div class="row">
                                <div class="col-md-6">
                                    <label for="firstName" class="form-label">First Name</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-user"></i></span>
                                        <input type="text" class="form-control profile-editable" id="firstName" name="username" value="<%= user ? user.username : '' %>" required readonly>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label for="lastName" class="form-label">Last Name</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-user-tag"></i></span>
                                        <input type="text" class="form-control profile-editable" id="lastName" name="lastName" value="<%= user ? user.lastName : '' %>" readonly>
                                    </div>
                                </div>
                            </div>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                                <input type="email" class="form-control profile-editable" id="email" name="email" value="<%= user ? user.email : '' %>" required readonly>
                            </div>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-phone"></i></span>
                                <input type="tel" class="form-control profile-editable" id="phone" name="phone" value="<%= user ? user.phone : '' %>" readonly>
                            </div>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-map-marker-alt"></i></span>
                                <textarea class="form-control profile-editable" id="address" name="address" rows="3" readonly><%= user ? user.address : '' %></textarea>
                            </div>
                            <div class="profile-actions">
                                <button type="button" class="btn btn-outline-secondary" id="edit-btn" onclick="enableEdit()">Edit</button>
                                <button type="button" class="btn btn-outline-secondary" id="cancel-btn" onclick="cancelEdit()" style="display:none;">Cancel</button>
                                <button type="submit" class="btn btn-primary" id="save-btn" style="display:none;">Save Changes</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>AgriMarket</h5>
                    <p>Fresh farm products delivered to your doorstep.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p>&copy; 2024 AgriMarket. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function enableEdit() {
            document.querySelectorAll('.profile-editable').forEach(el => el.removeAttribute('readonly'));
            document.getElementById('edit-btn').style.display = 'none';
            document.getElementById('save-btn').style.display = 'inline-block';
            document.getElementById('cancel-btn').style.display = 'inline-block';
        }
        function cancelEdit() {
            window.location.reload();
        }
    </script>
</body>
</html> 